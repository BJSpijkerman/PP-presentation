% Slide 1: 3D Point Source to Spatial Distribution
\begin{frame}{Heat transport}
	\centering
	\only<1>{
		\begin{equation*}
			\Delta T(\mathbf{r}, t) = \frac{T_0(\mathbf{r}')}{(4 \pi D t)^{3/2}} \exp\left[-\frac{|\mathbf{r}-\mathbf{r}'|^2}{4Dt}\right]
		\end{equation*}
	}
	
	\only<2>{
		\begin{equation*}
			\Delta T(\mathbf{r}, t) = \iiint_{-\infty}^{\infty} \frac{T_0(\mathbf{r}')}{(4 \pi D t)^{3/2}} \exp\left[-\frac{|\mathbf{r}-\mathbf{r}'|^2}{4Dt}\right] d^3\mathbf{r}'
		\end{equation*}
	}
\end{frame}

% Slide 2: Continuous Source Integration
\begin{frame}{Time-Dependent Continuous Source}
	\centering
	\small
	\only<1>{
		\begin{equation*}
			\Delta T(\mathbf{r}, t) = \int_{0}^{t} \iiint_{-\infty}^{\infty} \frac{dT(\mathbf{r}', t')}{dt'} \frac{1}{[4 \pi D (t-t')]^{3/2}} \exp\left[-\frac{|\mathbf{r}-\mathbf{r}'|^2}{4D(t-t')}\right] d^3\mathbf{r}' dt'
		\end{equation*}
	}
	\only<2>{
	\begin{equation*}
		\Delta T(\mathbf{r}, t) = \int_{0}^{t} \frac{1}{[4 \pi D (t-t')]^{3/2}} \left(\frac{dT(\mathbf{r}', t')}{dt'} *_{\mathbf{r}} \exp\left[-\frac{|\mathbf{r}-\mathbf{r}'|^2}{4D(t-t')}\right]\right)  dt'
	\end{equation*}
	}
\end{frame}



\pgfplotsset{compat=1.18}
\usepgfplotslibrary{fillbetween}

% Custom style to match your previous TikZ diagram
\pgfplotsset{
	waveguide_style/.style={
		width=8cm, height=5cm,
		axis lines=left,
		xlabel={$\mathbf{r}$},
		ylabel={$\Delta T$},
		label style={font=\small},
		tick label style={font=\tiny},
		legend style={font=\tiny, at={(0.95,0.95)}, anchor=north east},
		grid=both,
		grid style={line width=.1pt, draw=gray!10},
		no markers,
		every axis plot/.append style={line width=1pt}
	}
}

\begin{frame}{Continuous Source Integration}
	\centering
	\begin{tikzpicture}
		\begin{axis}[
			waveguide_style, 
			ylabel={Total $\Delta T$},
			ymin=0, % Ensures the plot starts at the baseline
			ymax=1.2 % Adjust based on your data peak
			]
			% 1. The Filled Area
			\addplot[name path=A, color=firebrick, thick] 
			table [x=x, y=delta_T, col sep=comma] {total_delta_t.csv};
			
			\path[name path=B] 
			(axis cs:\pgfkeysvalueof{/pgfplots/xmin},0) -- 
			(axis cs:\pgfkeysvalueof{/pgfplots/xmax},0);
			
			\addplot[orange!20] fill between[of=A and B];
			
			% 2. The Dashed Line (Source Location)
			% This pulls the 'initial' or 'source' column from your CSV
			\addplot[black, dashed, thin] 
			table [x=x, y=initial, col sep=comma] {diffusion_evolution.csv};
			\addlegendentry{Source Area}
			
		\end{axis}
	\end{tikzpicture}
\end{frame}

